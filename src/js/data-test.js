// 数据同步测试脚本\nconsole.log('=== 教师工具箱数据同步测试 ===');\n\n// 测试1: 检查localStorage中学生数据存储键名\nconsole.log('1. 检查学生数据存储键名...');\nconst storageKeys = Object.keys(localStorage);\nconst studentKey = storageKeys.find(key => key.includes('student') || key.includes('Student'));\nconsole.log('发现可能的学生数据键名:', studentKey || '未找到');\n\n// 测试2: 检查学生管理器\nconsole.log('\\n2. 检查学生管理器...');\nif (typeof StudentManager !== 'undefined') {\n    console.log('✓ StudentManager已加载');\n    console.log('✓ 存储键名:', StudentManager.STORAGE_KEY);\n    \n    // 尝试获取学生数据\n    try {\n        const students = StudentManager.getStudents();\n        console.log(`✓ 当前学生数: ${students.length}`);\n        console.log('✓ 学生数据格式正确');\n    } catch (e) {\n        console.log('✗ 获取学生数据时出错:', e.message);\n    }\n} else {\n    console.log('✗ StudentManager未加载');\n}\n\n// 测试3: 检查localStorage中的实际数据\nconsole.log('\\n3. 检查localStorage数据...');\nfor (let key in localStorage) {\n    if (typeof localStorage[key] === 'string' && (key.includes('student') || key.includes('Student'))) {\n        try {\n            const data = JSON.parse(localStorage[key]);\n            if (Array.isArray(data)) {\n                console.log(`✓ 键 \"${key}\" 包含 ${data.length} 条记录`);\n            }\n        } catch (e) {\n            // 不是JSON数据\n        }\n    }\n}\n\nconsole.log('\\n=== 测试完成 ===');